#include<bits/stdc++.h>
using namespace std;
struct student{
    string std_name;
    int id;
}s1;
int main()
{
    struct student *p;
    p=&s1;
    cin>>s1.std_name>>s1.id;
    cout<<p->std_name<<" "<<p->id<<endl;
    cout<<"////"<<endl;
    cout<<(*p).std_name<<" "<<(*p).id;
}
